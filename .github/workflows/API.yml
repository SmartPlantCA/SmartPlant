name: API

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:


jobs:
  deploy_api:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3
      - run: npm i
        working-directory: api
      - run: pm2 restart API
